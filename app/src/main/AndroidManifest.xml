<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <!-- Normal permission; some OEM BT stacks only deliver headset vendor broadcasts to apps that declare Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <!-- Android 12+ RFCOMM / SPP connect -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- BLE scan / connect (Android 12+); declared for merge; runtime on API 31+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <!-- BLE scan on many devices when targetSdk is high (API 23+ runtime) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />
    <application
        android:name=".HyTalkPttApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".MainActivity"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".PttKeySetupActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="false" />

        <!-- exported=true: some OEMs (e.g. Huawei) only deliver MEDIA_BUTTON to exported receivers -->
        <receiver
            android:name=".BluetoothMediaButtonReceiver"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <service
            android:name=".BluetoothPttService"
            android:exported="false" />

        <service
            android:name=".PTTAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
